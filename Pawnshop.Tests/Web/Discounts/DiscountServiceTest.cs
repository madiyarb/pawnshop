using Microsoft.VisualStudio.TestTools.UnitTesting;
using Pawnshop.AccountingCore.Models;
using Pawnshop.Data.Models.Contracts.Actions;
using System;
using System.Collections.Generic;
using System.Text;

namespace Pawnshop.Tests.Web.Discounts
{
    [TestClass()]
    public class DiscountServiceTest
    {
        /*[TestMethod()]
        public void ListOrderCheck()
        {
            List<DiscountRow> discountRows = new List<DiscountRow>();

            var disc1 = new DiscountRow() {
                PaymentType = AmountType.OverdueLoan,
                SubtractedCost = 1,
                OriginalCost = 1
            };
            //discountRows.Add(disc1);

            var disc2 = new DiscountRow()
            {
                PaymentType = AmountType.Loan,
                SubtractedCost = 2,
                OriginalCost = 2
            };
            //discountRows.Add(disc2);

            var disc3 = new DiscountRow()
            {
                PaymentType = AmountType.DebtPenalty,
                SubtractedCost = 3,
                OriginalCost = 3
            };
            //discountRows.Add(disc3);

            var disc4 = new DiscountRow()
            {
                PaymentType = AmountType.DebtPenalty,
                SubtractedCost = 4,
                OriginalCost = 4
            };
            //discountRows.Add(disc4);

            discountRows.Add(disc4);
            discountRows.Add(disc2);
            discountRows.Add(disc3);
            discountRows.Add(disc1);

            Assert.AreEqual(4, discountRows.Count);

        }
        */
    }
}
